{
  "name": "dmt-skale-vault",
  "version": "1.0.0",
  "description": "Dream Meme Token + Ownerless Vault — SKALE-native, gasless for users",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:skale": "hardhat run scripts/deploy.js --network skale"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.5"
  }
}
